// context.js
// create session (authorized user) context for the App
import React from 'react'

// initialize to null
const AuthUserContext = React.createContext(null)

export default AuthUserContext